# 🚀 Get Started

Welcome to **TailJNG**, a powerfull library components for Angular with Tailwind CSS integration. Continue with follow steps for start: 

---

## 1. Previous Requeriments

Before install **tailjng**, ensures that cumply with minimum requeriments 

- **Node.js**: Node.js version >= 18.19.1 ([descargar aquí](https://nodejs.org/)).
- **Angular CLI**:([instalar aquí](https://angular.io/cli)). Angular version >= 19
- **npm** o **yarn**: Package managements for dependecies installation

Verify your versions with executing:

```bash
node -v
npm -v
ng version
```
## 2. Installation

Install **angular animations**
```bash
npm install @angular/animations
```

Install **talwindcss**

```bash
npm install tailwindcss
```

Install **tailjng** in your Angular project with npm

```bash
npm install tailjng
```

## 3. How to use
Verify **available components**

```bash
npx tailjng list
```
Install a component. In this case is a **button**.

```bash
npx tailjng add button
```
or

Install **all available** components
```bash
npx tailjng install-all
```


